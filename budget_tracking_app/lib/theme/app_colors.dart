import 'package:flutter/material.dart';

class AppColors {
  // Semantic colors
  static const Color primary = Color(0xFF0DCDA3); // Vivid Teal
  static const Color secondary = Color(0xFF00BFA5); // Action/Accent Teal
  static const Color background = Color(0xFFF3FAF5); // Mint White background
  static const Color surface = Color(0xFFFFFFFF); // Pure White for cards
  static const Color textPrimary = Color(0xFF003D33); // Dark Teal text
  static const Color textSecondary = Color(0xFF4A6B63); // Muted Teal-Gray
  static const Color border = Color(0xFFD1E6DD); // Light Mint Border
  static const Color error = Color(0xFFFF3B30);

  // Additional design specific colors
  static const Color incomeAmount =
      Color(0xFF2196F3); // Blue for positive amounts or specific items
  static const Color expenseAmount =
      Color(0xFF2196F3); // The design shows expense as light blue too
  static const Color cardSecondary =
      Color(0xFFD1F2E6); // Light mint for inner cards
  static const Color headerDark =
      Color(0xFF043B33); // Dark teal for pill backgrounds
  static const Color iconBlue = Color(0xFF4C8DF5); // Blue background for icons
  static const Color iconBlueSecondary = Color(0xFF1E88E5);

  // Dark mode semantic colors
  static const Color darkBackground = Color(0xFF0B1917);
  static const Color darkSurface = Color(0xFF102622);
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFF98ACA7);
  static const Color darkBorder = Color(0xFF1F423C);
}
